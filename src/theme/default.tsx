const _buttons = {
  primary: {
    color: 'red',
    bg: 'blue',
  },
};

const _fontSizes = [];

const _colors = {
  background: 'black',
  primary: 'tomato',
};

const _space = [0, 4, 8, 12, 16, 32, 64];

const defaultTheme = {
  colors: {
    background: 'black',
    primary: 'tomato',
  },
};

export default defaultTheme;
